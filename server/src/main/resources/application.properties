spring.application.name=server

# Delay between each chunk when streaming /api/pi/stream (milliseconds; 0 disables).
app.pi-stream.chunk-delay-ms=45
# Bytes per write (larger = fewer chunks for the same π file).
app.pi-stream.chunk-size-bytes=1024
